let a=108;
let b=new Number(108);
let c=108;

console.log(a==b);//it compares only the content
console.log(b===c);//it compares the type of the variables
console.log(a===b);
console.log(a===c);

/*
  double equals to => compares only the content

   triple equals to => compare only the refence or the type of 
     variables.

*/